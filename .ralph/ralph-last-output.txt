<promise>READY_FOR_NEXT_TASK</promise>

OpenAI Codex v0.107.0 (research preview)
--------
workdir: /home/jonathan-aulson/projects/scriptStory
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: none
session id: 019cb06e-77a2-7fb1-b6ee-37572d51b5da
--------
user
# Ralph Wiggum Loop - Iteration 1

You are in an iterative development loop working through a task list.

## TASKS MODE: Working through task list

Current tasks from .ralph/ralph-tasks.md:
```markdown
# Ralph Tasks

## Guardrails (Non-Negotiable)
- Scope for this implementation is limited to:
  - Add additional AI providers: OpenAI and Anthropic.
  - Add configurable MCP servers for work-item writer context.
- Explicitly deferred (do not implement in this task set):
  - iOS App Store deployment work.
  - Android Play Store deployment work.
  - Jira Cloud integration work.
- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
- Optimize for token efficiency without degrading work-item quality:
  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
  - Manual "Enrich Context" path must remain available for high-fidelity cases.
- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
- Secrets must never be committed to git; use environment variables and local non-committed config files only.

- [ ] Epic: Constraint Check (Guardrails)
  - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
  - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
  - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
  - [ ] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
  - [ ] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.

- [ ] Epic: Create Local API Runtime For Provider + MCP Orchestration
  - [ ] Create `server/` TypeScript runtime (Node + Express/Fastify) and add scripts for `dev:server` and combined `dev` workflow.
  - [ ] Configure Vite proxy so browser calls `/api/*` locally without CORS breakage.
  - [ ] Add health endpoint (`GET /api/health`) and structured error envelope for all API responses.
  - [ ] Bind server to loopback-only (`127.0.0.1`) by default for single-user local MVP.
  - [ ] Add secure environment loading for provider keys (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, existing Gemini key fallback) from local env files.
  - [ ] Add local persistence folder `.local-data/` (gitignored) for single-user config payloads (MCP registry, defaults, cache metadata).
  - [ ] Acceptance Criteria: App runs locally with frontend + backend together; no provider secret is read directly by browser code.

- [ ] Epic: Introduce AI Provider Abstraction (Frontend + Backend)
  - [ ] Define provider contracts:
    - `WriterProvider`: summarize transcript, analyze transcript into tool calls, optional command session.
    - `TranscriptionProvider`: realtime/near-realtime transcript capture capabilities.
  - [ ] Add provider capability matrix (realtime audio, streaming text, tool-call support, strict JSON mode).
  - [ ] Refactor current Gemini logic in `services/geminiLiveService.ts` behind adapter interfaces without changing output behavior.
  - [ ] Add backend provider router (`/api/ai/*`) that dispatches to selected provider adapter.
  - [ ] Add feature flags for incremental rollout (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`) with safe defaults.
  - [ ] Add provider selection model in app state/local storage with safe defaults (`Gemini` for transcription, configurable writer provider).
  - [ ] Acceptance Criteria: Existing workflows still function after abstraction; provider can be swapped by config instead of hard-coded imports.

- [ ] Epic: Add OpenAI Provider Adapter
  - [ ] Implement backend OpenAI adapter for writer operations:
    - Transcript summarization.
    - Transcript analysis into tool invocations (`createWorkItem`, `updateWorkItem`, `deleteWorkItem`, etc.).
  - [ ] Implement streaming/tool-call handling and schema validation so tool arguments match existing `types.ts` contracts.
  - [ ] Add retry/backoff and model fallback strategy for transient OpenAI failures.
  - [ ] Add provider-specific config fields in settings (model IDs, temperature, max output tokens).
  - [ ] Add integration tests with fixed transcript fixtures validating quality and deterministic tool-call extraction shape.
  - [ ] Acceptance Criteria: User can choose OpenAI writer provider and produce valid work-item outputs equivalent to Gemini baseline quality.

- [ ] Epic: Add Anthropic Provider Adapter
  - [ ] Implement backend Anthropic adapter for writer operations:
    - Transcript summarization.
    - Transcript analysis into tool invocations.
  - [ ] Define fallback behavior for non-realtime transcription scenarios (keep Gemini transcription path while Anthropic handles writer tasks).
  - [ ] Implement tool-use parsing/validation and adapter normalization into shared tool-call objects.
  - [ ] Implement streaming response support for long analysis calls and safe truncation behavior.
  - [ ] Add provider-specific config fields in settings (model IDs, temperature, max output tokens).
  - [ ] Add integration tests with same transcript fixtures used for Gemini/OpenAI to compare extraction fidelity.
  - [ ] Acceptance Criteria: User can choose Anthropic writer provider and complete end-to-end work-item generation without schema regressions.

- [ ] Epic: Build MCP Gateway + Server Registry (Single-User Local MVP)
  - [ ] Add backend MCP gateway service that is the only component allowed to communicate with MCP servers.
  - [ ] Add MCP registry schema for arbitrary servers:
    - `id`, `name`, `transport`, `endpointOrCommand`, `auth`, `enabled`, `priority`, `timeouts`, `maxPayload`, `allowedResources`.
  - [ ] Persist registry in `.local-data/mcp-servers.json` with schema version for future migration.
  - [ ] Add registry CRUD endpoints (`GET/POST/PATCH/DELETE /api/mcp/servers`) and test endpoint (`POST /api/mcp/servers/:id/test`).
  - [ ] Add auth-secret handling for MCP server configs (store secrets outside committed files and redact values in logs/API responses).
  - [ ] Add context sanitation pipeline for MCP results (truncate oversized payloads, strip unsafe markup, normalize to plain text with source metadata).
  - [ ] Add per-server circuit breaker state and health tracking to avoid repeated failures.
  - [ ] Acceptance Criteria: Single local user can add arbitrary MCP server configs, test connectivity, enable/disable servers, and persist state across restarts.

- [ ] Epic: Implement Token-Efficient Context Retrieval Strategy (Recommended Hybrid)
  - [ ] Implement default `Auto-Smart Retrieval` policy:
    - Stage 1: low-token intent classification decides whether external context is needed.
    - Stage 2: fetch candidates from enabled MCP servers, rank, and include only top snippets under strict token budget.
  - [ ] Implement manual `Enrich Context` path to force deeper retrieval for difficult/ambiguous items.
  - [ ] Add confidence-based escalation: if initial generation confidence/validation is low, perform a second pass with expanded context budget before returning output.
  - [ ] Implement budget controller with explicit caps:
    - Global per-request context token budget.
    - Per-server quota cap.
    - Hard ceiling on attached snippet count and snippet length.
  - [ ] Add cache for MCP retrieval results (query hash + TTL) to reduce repeated token spend across iterative edits.
  - [ ] Add citation objects in tool-call metadata so each generated work-item can show which MCP source snippets influenced output.
  - [ ] Acceptance Criteria: Default flow minimizes token usage via bounded retrieval, while manual enrichment can increase fidelity on demand; citations are available for generated items.

- [ ] Epic: Add UI For Provider + MCP Configuration + Context Controls
  - [ ] Extend Settings modal in `App.tsx` with new tabs:
    - `AI Providers`.
    - `MCP Servers`.
    - `Context Policy`.
  - [ ] Add provider selector for writer/transcription roles with model-level config fields.
  - [ ] Add MCP server management UI:
    - Create/edit/delete server entries.
    - Enable/disable toggles.
    - Priority ordering.
    - Connection test action with status output.
  - [ ] Add context policy UI controls:
    - Auto-Smart (default).
    - Manual Enrich action.
    - Token budget sliders/inputs.
  - [ ] Add generated-item context trace view (source citations and retrieval stats) in work-item card details or side panel.
  - [ ] Acceptance Criteria: User can fully configure providers and MCP context behavior from UI without code edits or direct file edits.

- [ ] Epic: Quality, Regression, and Cost Validation
  - [ ] Build transcript fixture suite covering bug reports, stories/features, mixed meeting chatter, and ambiguous requirements.
  - [ ] Add regression checks comparing provider outputs to expected tool-call structure and minimum quality rubric.
  - [ ] Add token usage telemetry per request/provider/server and expose aggregate stats in a local diagnostics panel.
  - [ ] Add failure-mode tests (MCP offline, invalid auth, provider timeout, malformed tool args) and verify graceful fallback behavior.
  - [ ] Add feature-flag regression checks proving app remains stable with new capabilities disabled.
  - [ ] Acceptance Criteria: Provider + MCP features pass regression suite, quality rubric, and token-budget compliance checks with graceful degradation on failures.

- [ ] Epic: Future Evolution Hooks (Per-User/Per-Workspace Readiness Without Implementing Multi-User Now)
  - [ ] Introduce config domain model with explicit ownership fields (`scopeType`, `scopeId`) while using fixed local single-user defaults for MVP.
  - [ ] Add migration-ready storage interfaces so `.local-data` persistence can be replaced later by database-backed per-user/per-workspace storage.
  - [ ] Document upgrade path in `docs/multi-tenant-evolution.md`:
    - What data moves to workspace scope.
    - What stays user scope.
    - Backward compatibility migration steps.
  - [ ] Acceptance Criteria: MVP remains single-user local, but storage/contracts are structured to add user/workspace scoping without breaking existing configs.
```

📍 NEXT TASK: "Epic: Constraint Check (Guardrails)"
   Mark as [/] in .ralph/ralph-tasks.md before starting.
   When done: Mark as [x] and output <promise>READY_FOR_NEXT_TASK</promise>

### Task Workflow
1. Find any task marked [/] (in progress). If none, pick the first [ ] task.
2. Mark the task as [/] in ralph-tasks.md before starting.
3. Complete the task.
4. Mark as [x] when verified complete.
5. Output <promise>READY_FOR_NEXT_TASK</promise> to move to the next task.
6. Only output <promise>COMPLETE</promise> when ALL tasks are [x].

---

## Your Main Goal

Notify only when all tasks are done.

## Critical Rules

- Work on ONE task at a time from .ralph/ralph-tasks.md
- ONLY output <promise>READY_FOR_NEXT_TASK</promise> when the current task is complete and marked in ralph-tasks.md
- ONLY output <promise>COMPLETE</promise> when ALL tasks are truly done
- Output promise tags DIRECTLY - do not quote them, explain them, or say you "will" output them
- Do NOT lie or output false promises to exit the loop
- If stuck, try a different approach
- Check your work before claiming completion

## Current Iteration: 1 (unlimited) (min: 1)

Tasks Mode: ENABLED - Work on one task at a time from ralph-tasks.md

Now, work on the current task. Good luck!
mcp: context7 starting
mcp: playwright starting
mcp: openaiDeveloperDocs starting
mcp: context7 ready
mcp: openaiDeveloperDocs ready
mcp: playwright ready
mcp startup: ready: context7, openaiDeveloperDocs, playwright

thinking
**Starting Epic Constraint Inspection**
codex
I’m taking the first task, `Epic: Constraint Check (Guardrails)`. I’ll mark it in-progress in `.ralph/ralph-tasks.md`, then inspect the repo for existing scripts and config so I can add guardrail docs + an enforceable `check-guardrails` flow with minimal disruption.
exec
/bin/bash -lc 'git status --short' in /home/jonathan-aulson/projects/scriptStoryexec
/bin/bash -lc "rg --files -g 'package.json' -g '.ralph/**' -g 'scripts/**' -g 'docs/**'" in /home/jonathan-aulson/projects/scriptStoryexec
/bin/bash -lc 'ls -la' in /home/jonathan-aulson/projects/scriptStory succeeded in 50ms:
 D .env.local:Zone.Identifier
 D .gitignore:Zone.Identifier
 D App.tsx:Zone.Identifier
 D README.md:Zone.Identifier
 D components/Visualizer.tsx:Zone.Identifier
 D components/WorkItemCard.tsx:Zone.Identifier
 D index.html:Zone.Identifier
 D index.tsx:Zone.Identifier
 D metadata.json:Zone.Identifier
 D package.json:Zone.Identifier
 D services/adoService.ts:Zone.Identifier
 D services/audioUtils.ts:Zone.Identifier
 D services/documentUtils.ts:Zone.Identifier
 D services/geminiLiveService.ts:Zone.Identifier
 D tsconfig.json:Zone.Identifier
 D types.ts:Zone.Identifier
 D vite.config.ts:Zone.Identifier
?? .ralph/
 succeeded in 50ms:
package.json
 succeeded in 51ms:
total 104
drwxr-xr-x  6 jonathan-aulson jonathan-aulson  4096 Mar  2 15:09 .
drwxr-xr-x 32 jonathan-aulson jonathan-aulson  4096 Feb 25 17:16 ..
-rw-r--r--  1 jonathan-aulson jonathan-aulson    35 Feb 20 12:16 .env.local
drwxr-xr-x  8 jonathan-aulson jonathan-aulson  4096 Mar  2 15:22 .git
-rw-r--r--  1 jonathan-aulson jonathan-aulson   253 Feb 20 12:16 .gitignore
drwxr-xr-x  2 jonathan-aulson jonathan-aulson  4096 Mar  2 15:22 .ralph
-rw-r--r--  1 jonathan-aulson jonathan-aulson 38482 Feb 20 12:16 App.tsx
-rw-r--r--  1 jonathan-aulson jonathan-aulson   550 Feb 20 12:16 README.md
drwxr-xr-x  2 jonathan-aulson jonathan-aulson  4096 Feb 20 12:39 components
-rw-r--r--  1 jonathan-aulson jonathan-aulson  1622 Feb 20 12:16 index.html
-rw-r--r--  1 jonathan-aulson jonathan-aulson   349 Feb 20 12:16 index.tsx
-rw-r--r--  1 jonathan-aulson jonathan-aulson   217 Feb 20 12:16 metadata.json
-rw-r--r--  1 jonathan-aulson jonathan-aulson   488 Feb 20 12:16 package.json
drwxr-xr-x  2 jonathan-aulson jonathan-aulson  4096 Feb 20 12:39 services
-rw-r--r--  1 jonathan-aulson jonathan-aulson   542 Feb 20 12:16 tsconfig.json
-rw-r--r--  1 jonathan-aulson jonathan-aulson  3251 Feb 20 12:16 types.ts
-rw-r--r--  1 jonathan-aulson jonathan-aulson   580 Feb 20 12:16 vite.config.ts

thinking
**Inspecting Windows-specific artifacts**
codex
I found the repo is small and currently has no `scripts/` or `docs/` folders yet. Next I’m opening `.ralph/ralph-tasks.md` and `package.json`, then I’ll mark the guardrails epic as in progress and scaffold the new checks.
exec
/bin/bash -lc 'cat package.json' in /home/jonathan-aulson/projects/scriptStoryexec
/bin/bash -lc "sed -n '1,260p' .ralph/ralph-tasks.md" in /home/jonathan-aulson/projects/scriptStory succeeded in 51ms:
{
  "name": "transkriboida",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.30.0",
    "react-dom": "^19.2.0",
    "react": "^19.2.0",
    "uuid": "^13.0.0",
    "mammoth": "1.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
 succeeded in 50ms:
# Ralph Tasks

## Guardrails (Non-Negotiable)
- Scope for this implementation is limited to:
  - Add additional AI providers: OpenAI and Anthropic.
  - Add configurable MCP servers for work-item writer context.
- Explicitly deferred (do not implement in this task set):
  - iOS App Store deployment work.
  - Android Play Store deployment work.
  - Jira Cloud integration work.
- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
- Optimize for token efficiency without degrading work-item quality:
  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
  - Manual "Enrich Context" path must remain available for high-fidelity cases.
- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
- Secrets must never be committed to git; use environment variables and local non-committed config files only.

- [ ] Epic: Constraint Check (Guardrails)

thinking
**Starting task with patch apply**
codex
I’m now marking the guardrails epic as `[/]` in `.ralph/ralph-tasks.md`, then I’ll implement the docs/script/package wiring and verify the guardrail check runs locally.
file update
M /home/jonathan-aulson/projects/scriptStory/.ralph/ralph-tasks.md
@@ -19,3 +19,3 @@
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
apply_patch(auto_approved=true) exited 0 in 9ms:
Success. Updated the following files:
M /home/jonathan-aulson/projects/scriptStory/.ralph/ralph-tasks.md
file update:
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).

file update:
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).


thinking
**Designing guardrail scaffolding**
exec
/bin/bash -lc 'cat .gitignore && git ls-files .env.local .env .local-data 2>/dev/null' in /home/jonathan-aulson/projects/scriptStory succeeded in 51ms:
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
file update:
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).


thinking
**Establishing guardrails scaffolding and verification**
codex
I’ve marked the epic in progress. I’m now checking what existing config/security scaffolding already exists so the guardrail script can enforce real paths/flags without breaking the current app.
exec
/bin/bash -lc 'rg -n "sanitize|sanitiz|ENABLE_OPENAI_WRITER|ENABLE_ANTHROPIC_WRITER|ENABLE_MCP_CONTEXT|feature flag|featureFlag|flag" App.tsx services types.ts index.tsx vite.config.ts README.md' in /home/jonathan-aulson/projects/scriptStoryexec
/bin/bash -lc 'rg --files' in /home/jonathan-aulson/projects/scriptStory exited 1 in 50ms:

 succeeded in 51ms:
index.html
index.tsx
services/audioUtils.ts
services/adoService.ts
services/geminiLiveService.ts
services/documentUtils.ts
vite.config.ts
metadata.json
types.ts
App.tsx
README.md
tsconfig.json
package.json
components/WorkItemCard.tsx
components/Visualizer.tsx
file update:
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).


thinking
**Inspecting project structure and docs**
exec
/bin/bash -lc 'cat README.md' in /home/jonathan-aulson/projects/scriptStory succeeded in 51ms:
<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# Run and deploy your AI Studio app

This contains everything you need to run your app locally.

View your app in AI Studio: https://ai.studio/apps/785a7d68-2839-48b6-b4b2-8558f5ecf242

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
file update:
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).


thinking
**Designing secrets and feature flag checks**
exec
/bin/bash -lc 'cat tsconfig.json' in /home/jonathan-aulson/projects/scriptStory succeeded in 50ms:
{
  "compilerOptions": {
    "target": "ES2022",
    "experimentalDecorators": true,
    "useDefineForClassFields": false,
    "module": "ESNext",
    "lib": [
      "ES2022",
      "DOM",
      "DOM.Iterable"
    ],
    "skipLibCheck": true,
    "types": [
      "node"
    ],
    "moduleResolution": "bundler",
    "isolatedModules": true,
    "moduleDetection": "force",
    "allowJs": true,
    "jsx": "react-jsx",
file update:
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).



thinking
**Designing comprehensive guardrails script and docs**
thinking
**Refining config and sanitization with docs and verification**
codex
I’m implementing the guardrail artifacts now: creating `docs/guardrails.md`, adding `scripts/check-guardrails.sh`, scaffolding explicit feature flags + MCP sanitization path, and wiring `npm` verification scripts so this is enforceable locally.
file update
M /home/jonathan-aulson/projects/scriptStory/.gitignore
@@ -13,2 +13,6 @@
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
apply_patch(auto_approved=true) exited 0 in 13ms:
Success. Updated the following files:
M /home/jonathan-aulson/projects/scriptStory/.gitignore
file update:
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).

file update:
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).


thinking
**Committing file additions and documentation updates**
file update
A /home/jonathan-aulson/projects/scriptStory/docs/guardrails.md
# Guardrails

## Non-Negotiable Constraints
- Scope for this implementation is limited to:
  - Add additional AI providers: OpenAI and Anthropic.
  - Add configurable MCP servers for work-item writer context.
- Explicitly deferred (do not implement in this task set):
  - iOS App Store deployment work.
  - Android Play Store deployment work.
  - Jira Cloud integration work.
- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
- Optimize for token efficiency without degrading work-item quality:
  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
  - Manual "Enrich Context" path must remain available for high-fidelity cases.
- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
- Secrets must never be committed to git; use environment variables and local non-committed config files only.

## Guardrail Enforcement
- Run `npm run check:guardrails` before merge.
- `scripts/check-guardrails.sh` blocks:
  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
  - Committed secrets and tracked environment files.
  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).

## Local Verification
- Lightweight guardrail check: `npm run check:guardrails`.
- Full local verification flow: `npm run verify`.
apply_patch(auto_approved=true) exited 0 in 12ms:
Success. Updated the following files:
A /home/jonathan-aulson/projects/scriptStory/docs/guardrails.md
file update:
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.

file update:
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.

file update
A /home/jonathan-aulson/projects/scriptStory/.env.example
# Provider API keys (local-only values, never commit real secrets)
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Feature flags (safe defaults)
ENABLE_OPENAI_WRITER=false
ENABLE_ANTHROPIC_WRITER=false
ENABLE_MCP_CONTEXT=false
apply_patch(auto_approved=true) exited 0 in 24ms:
Success. Updated the following files:
A /home/jonathan-aulson/projects/scriptStory/.env.example
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.

file update
A /home/jonathan-aulson/projects/scriptStory/config/featureFlags.ts
export interface FeatureFlags {
  ENABLE_OPENAI_WRITER: boolean;
  ENABLE_ANTHROPIC_WRITER: boolean;
  ENABLE_MCP_CONTEXT: boolean;
}

const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
  if (value === undefined || value.trim() === "") {
    return fallback;
  }

  return value.trim().toLowerCase() === "true";
};

export const featureFlags: FeatureFlags = Object.freeze({
  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
});
apply_patch(auto_approved=true) exited 0 in 15ms:
Success. Updated the following files:
A /home/jonathan-aulson/projects/scriptStory/config/featureFlags.ts
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.

file update
A /home/jonathan-aulson/projects/scriptStory/server/mcp/sanitizeContext.ts
export interface McpSourceTag {
  serverId: string;
  serverName?: string;
  resource?: string;
}

export interface McpRawContextPayload {
  source: McpSourceTag;
  content: unknown;
}

export interface SanitizedMcpContextSnippet {
  source: McpSourceTag;
  text: string;
  truncated: boolean;
}

const DEFAULT_MAX_CHARS = 4_000;
const MARKUP_PATTERN = /<[^>]*>/g;
const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
const WHITESPACE_PATTERN = /\s+/g;

const toText = (value: unknown): string => {
  if (typeof value === "string") {
    return value;
  }

  if (value === null || value === undefined) {
    return "";
  }

  try {
    return JSON.stringify(value, null, 2);
  } catch {
    return String(value);
  }
};

const normalizeText = (input: string): string => {
  return input
    .replace(MARKUP_PATTERN, " ")
    .replace(CONTROL_CHAR_PATTERN, " ")
    .replace(WHITESPACE_PATTERN, " ")
    .trim();
};

export function sanitizeMcpContext(
  payload: McpRawContextPayload,
  maxChars = DEFAULT_MAX_CHARS,
): SanitizedMcpContextSnippet {
  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
  const normalized = normalizeText(toText(payload.content));
  const bounded = normalized.slice(0, budget);

  return {
    source: payload.source,
    text: bounded,
    truncated: normalized.length > bounded.length,
  };
}
apply_patch(auto_approved=true) exited 0 in 14ms:
Success. Updated the following files:
A /home/jonathan-aulson/projects/scriptStory/server/mcp/sanitizeContext.ts
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update
A /home/jonathan-aulson/projects/scriptStory/scripts/check-guardrails.sh
#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

FAILED=0

record_error() {
  FAILED=1
  printf '[guardrails] ERROR: %s\n' "$1" >&2
}

record_error_block() {
  FAILED=1
  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
}

require_file() {
  local path="$1"
  if [[ ! -f "$path" ]]; then
    record_error "Missing required file: $path"
  fi
}

check_forbidden_scope_artifacts() {
  local path_matches
  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
  if [[ -n "$path_matches" ]]; then
    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
  fi

  local capacitor_deps
  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
  if [[ -n "$capacitor_deps" ]]; then
    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
  fi

  local jira_deps
  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
  if [[ -n "$jira_deps" ]]; then
    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
  fi
}

check_secret_hygiene() {
  local tracked_env_files
  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
  if [[ -n "$tracked_env_files" ]]; then
    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
  fi

  local secret_matches
  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
  if [[ -n "$secret_matches" ]]; then
    record_error_block "Possible committed secret(s) detected." "$secret_matches"
  fi
}

check_feature_flags() {
  require_file "config/featureFlags.ts"
  require_file ".env.example"

  local missing_entries=()
  local flag
  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
    if ! rg -q "$flag" config/featureFlags.ts; then
      missing_entries+=("config/featureFlags.ts missing ${flag}")
    fi

    if ! rg -q "^${flag}=false$" .env.example; then
      missing_entries+=(".env.example missing ${flag}=false")
    fi
  done

  if ((${#missing_entries[@]} > 0)); then
    local details=""
    local item
    for item in "${missing_entries[@]}"; do
      details+="- ${item}"$'\n'
    done
    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
  fi
}

check_mcp_sanitization_path() {
  require_file "server/mcp/sanitizeContext.ts"

  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
    fi

    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
      record_error "MCP sanitization output must remain source-tagged."
    fi

    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
      record_error "MCP sanitization output must remain bounded and truncation-aware."
    fi
  fi
}

check_guardrail_docs() {
  require_file "docs/guardrails.md"
}

check_guardrail_docs
check_forbidden_scope_artifacts
check_secret_hygiene
check_feature_flags
check_mcp_sanitization_path

if [[ "$FAILED" -ne 0 ]]; then
  printf '[guardrails] Validation failed.\n' >&2
  exit 1
fi

printf '[guardrails] All guardrail checks passed.\n'
apply_patch(auto_approved=true) exited 0 in 15ms:
Success. Updated the following files:
A /home/jonathan-aulson/projects/scriptStory/scripts/check-guardrails.sh
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100644
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100644
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update
M /home/jonathan-aulson/projects/scriptStory/package.json
@@ -7,2 +7,5 @@
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
apply_patch(auto_approved=true) exited 0 in 11ms:
Success. Updated the following files:
M /home/jonathan-aulson/projects/scriptStory/package.json
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100644
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100644
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update
M /home/jonathan-aulson/projects/scriptStory/README.md
@@ -20 +20,6 @@
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
apply_patch(auto_approved=true) exited 0 in 14ms:
Success. Updated the following files:
M /home/jonathan-aulson/projects/scriptStory/README.md
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100644
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100644
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}


thinking
**Making script executable**
exec
/bin/bash -lc 'chmod +x scripts/check-guardrails.sh' in /home/jonathan-aulson/projects/scriptStory succeeded in 51ms:

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}


thinking
**Preparing to run guardrail checks**
codex
The guardrail files and scripts are in place; next I’m running the new check command and then reviewing the task list file to mark the epic complete only if validation passes.
exec
/bin/bash -lc 'git status --short' in /home/jonathan-aulson/projects/scriptStory succeeded in 51ms:
 D .env.local:Zone.Identifier
 M .gitignore
 D .gitignore:Zone.Identifier
 D App.tsx:Zone.Identifier
 M README.md
 D README.md:Zone.Identifier
 D components/Visualizer.tsx:Zone.Identifier
 D components/WorkItemCard.tsx:Zone.Identifier
 D index.html:Zone.Identifier
 D index.tsx:Zone.Identifier
 D metadata.json:Zone.Identifier
 M package.json
 D package.json:Zone.Identifier
 D services/adoService.ts:Zone.Identifier
 D services/audioUtils.ts:Zone.Identifier
 D services/documentUtils.ts:Zone.Identifier
 D services/geminiLiveService.ts:Zone.Identifier
 D tsconfig.json:Zone.Identifier
 D types.ts:Zone.Identifier
 D vite.config.ts:Zone.Identifier
exec
/bin/bash -lc 'npm run check:guardrails' in /home/jonathan-aulson/projects/scriptStory exited 1 in 58ms:
[guardrails] Validation failed.
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..74b48d415bd2f3e6610198ee6737d3cc824ac2e3
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,119 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}


thinking
**Ignoring Windows stream artifact paths**
file update
M /home/jonathan-aulson/projects/scriptStory/scripts/check-guardrails.sh
@@ -25,5 +25,9 @@
 
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
 check_forbidden_scope_artifacts() {
   local path_matches
-  path_matches="$(git ls-files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
   if [[ -n "$path_matches" ]]; then
@@ -47,3 +51,3 @@
   local tracked_env_files
-  tracked_env_files="$(git ls-files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
   if [[ -n "$tracked_env_files" ]]; then
apply_patch(auto_approved=true) exited 0 in 15ms:
Success. Updated the following files:
M /home/jonathan-aulson/projects/scriptStory/scripts/check-guardrails.sh
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..7e156e6ebb0d75e8aad6607b6556374ba90988ee
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,123 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..7e156e6ebb0d75e8aad6607b6556374ba90988ee
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,123 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

exec
/bin/bash -lc 'npm run check:guardrails' in /home/jonathan-aulson/projects/scriptStory succeeded in 51ms:

> transkriboida@0.0.0 check:guardrails
> bash ./scripts/check-guardrails.sh

[guardrails] All guardrail checks passed.
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..7e156e6ebb0d75e8aad6607b6556374ba90988ee
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,123 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}


thinking
**Running local verification script**
exec
/bin/bash -lc 'npm run verify' in /home/jonathan-aulson/projects/scriptStory exited 127 in 253ms:

> transkriboida@0.0.0 check:guardrails
> bash ./scripts/check-guardrails.sh

[guardrails] All guardrail checks passed.

> transkriboida@0.0.0 build
> vite build

sh: 1: vite: not found
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..331426d9706e11818f4ed6aa2ef4fb1c80bca4cf
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,7 +17,7 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
+- [/] Epic: Constraint Check (Guardrails)
   - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
   - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
   - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..7e156e6ebb0d75e8aad6607b6556374ba90988ee
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,123 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}


thinking
**Planning task and epic status update**
codex
Guardrail checks pass with `npm run check:guardrails`. I’m now updating `.ralph/ralph-tasks.md` to mark the guardrails epic and its checklist complete, then I’ll report completion for this iteration.
file update
M /home/jonathan-aulson/projects/scriptStory/.ralph/ralph-tasks.md
@@ -19,8 +19,8 @@
 
-- [/] Epic: Constraint Check (Guardrails)
-  - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
-  - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
-  - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
-  - [ ] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
-  - [ ] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
+- [x] Epic: Constraint Check (Guardrails)
+  - [x] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
+  - [x] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
+  - [x] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
+  - [x] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
+  - [x] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
 
apply_patch(auto_approved=true) exited 0 in 19ms:
Success. Updated the following files:
M /home/jonathan-aulson/projects/scriptStory/.ralph/ralph-tasks.md
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..50e37dc1e9d1d3e2eefc6ca314a674917021965e
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,12 +17,12 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
-  - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
-  - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
-  - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
-  - [ ] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
-  - [ ] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
+- [x] Epic: Constraint Check (Guardrails)
+  - [x] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
+  - [x] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
+  - [x] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
+  - [x] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
+  - [x] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
 
 - [ ] Epic: Create Local API Runtime For Provider + MCP Orchestration
   - [ ] Create `server/` TypeScript runtime (Node + Express/Fastify) and add scripts for `dev:server` and combined `dev` workflow.
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..7e156e6ebb0d75e8aad6607b6556374ba90988ee
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,123 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..50e37dc1e9d1d3e2eefc6ca314a674917021965e
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,12 +17,12 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
-  - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
-  - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
-  - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
-  - [ ] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
-  - [ ] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
+- [x] Epic: Constraint Check (Guardrails)
+  - [x] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
+  - [x] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
+  - [x] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
+  - [x] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
+  - [x] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
 
 - [ ] Epic: Create Local API Runtime For Provider + MCP Orchestration
   - [ ] Create `server/` TypeScript runtime (Node + Express/Fastify) and add scripts for `dev:server` and combined `dev` workflow.
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..7e156e6ebb0d75e8aad6607b6556374ba90988ee
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,123 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}


thinking
**Confirming final output format**
codex
<promise>READY_FOR_NEXT_TASK</promise>
file update:
diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000000000000000000000000000000000000..aeb2eb6c463641d13a6964ab534ab8f003d020e8
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,9 @@
+# Provider API keys (local-only values, never commit real secrets)
+GEMINI_API_KEY=
+OPENAI_API_KEY=
+ANTHROPIC_API_KEY=
+
+# Feature flags (safe defaults)
+ENABLE_OPENAI_WRITER=false
+ENABLE_ANTHROPIC_WRITER=false
+ENABLE_MCP_CONTEXT=false
diff --git a/.gitignore b/.gitignore
index a547bf36d8d11a4f89c59c144f24795749086dd1..84c7f9ae8d26c9a69b41b428f63b7f5b0b830619
--- a/.gitignore
+++ b/.gitignore
@@ -11,6 +11,10 @@
 dist
 dist-ssr
 *.local
+.env
+.env.*
+!.env.example
+.local-data/
 
 # Editor directories and files
 .vscode/*
diff --git a/.ralph/ralph-tasks.md b/.ralph/ralph-tasks.md
index bc56b22a32c7a6cd35ecf450bdb5abc51daa39a9..50e37dc1e9d1d3e2eefc6ca314a674917021965e
--- a/.ralph/ralph-tasks.md
+++ b/.ralph/ralph-tasks.md
@@ -17,12 +17,12 @@
 - Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
 - Secrets must never be committed to git; use environment variables and local non-committed config files only.
 
-- [ ] Epic: Constraint Check (Guardrails)
-  - [ ] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
-  - [ ] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
-  - [ ] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
-  - [ ] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
-  - [ ] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
+- [x] Epic: Constraint Check (Guardrails)
+  - [x] Add `docs/guardrails.md` mirroring the non-negotiable constraints and deferred scope.
+  - [x] Add a lightweight guardrail validation script `scripts/check-guardrails.sh` that fails on forbidden scope introduction (Capacitor/iOS/Android/Jira artifacts) in this milestone branch.
+  - [x] Add guardrail checks for security constraints (no committed secrets, MCP prompt/context sanitization path present, and provider + MCP features behind explicit flags).
+  - [x] Add `npm run check:guardrails` in `package.json` and wire into local verification flow.
+  - [x] Acceptance Criteria: Guardrails are documented, scriptable, and enforced before merge; deferred scope artifacts are blocked.
 
 - [ ] Epic: Create Local API Runtime For Provider + MCP Orchestration
   - [ ] Create `server/` TypeScript runtime (Node + Express/Fastify) and add scripts for `dev:server` and combined `dev` workflow.
diff --git a/README.md b/README.md
index 9e355ce648aa22eff3f36ebb14a8632e272f52a8..c210b968d4ccc81f5ab765cf7806f7b4d73b307c
--- a/README.md
+++ b/README.md
@@ -18,3 +18,8 @@
 2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
 3. Run the app:
    `npm run dev`
+
+## Local verification
+
+- Guardrails only: `npm run check:guardrails`
+- Full local verification: `npm run verify`
diff --git a/config/featureFlags.ts b/config/featureFlags.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b49a3019413401cf581b848dbe9f566ad0ccef05
--- /dev/null
+++ b/config/featureFlags.ts
@@ -0,0 +1,19 @@
+export interface FeatureFlags {
+  ENABLE_OPENAI_WRITER: boolean;
+  ENABLE_ANTHROPIC_WRITER: boolean;
+  ENABLE_MCP_CONTEXT: boolean;
+}
+
+const readBooleanFlag = (value: string | undefined, fallback = false): boolean => {
+  if (value === undefined || value.trim() === "") {
+    return fallback;
+  }
+
+  return value.trim().toLowerCase() === "true";
+};
+
+export const featureFlags: FeatureFlags = Object.freeze({
+  ENABLE_OPENAI_WRITER: readBooleanFlag(process.env.ENABLE_OPENAI_WRITER, false),
+  ENABLE_ANTHROPIC_WRITER: readBooleanFlag(process.env.ENABLE_ANTHROPIC_WRITER, false),
+  ENABLE_MCP_CONTEXT: readBooleanFlag(process.env.ENABLE_MCP_CONTEXT, false),
+});
diff --git a/docs/guardrails.md b/docs/guardrails.md
new file mode 100644
index 0000000000000000000000000000000000000000..8e63d274f780e2b72862c8f7af83726ee5d2135f
--- /dev/null
+++ b/docs/guardrails.md
@@ -0,0 +1,30 @@
+# Guardrails
+
+## Non-Negotiable Constraints
+- Scope for this implementation is limited to:
+  - Add additional AI providers: OpenAI and Anthropic.
+  - Add configurable MCP servers for work-item writer context.
+- Explicitly deferred (do not implement in this task set):
+  - iOS App Store deployment work.
+  - Android Play Store deployment work.
+  - Jira Cloud integration work.
+- MVP target is single-user local execution only (existing app operating model); design must leave a clean migration path to per-user/per-workspace configuration.
+- MCP server configuration may accept arbitrary endpoints/commands, but execution must still apply safety controls (timeouts, payload limits, audit logs, and explicit enable/disable controls).
+- MCP-sourced content is untrusted input and must be sanitized, source-tagged, and bounded before entering model prompts or UI rendering.
+- Optimize for token efficiency without degrading work-item quality:
+  - Default strategy is hybrid "Auto-Smart Retrieval" with bounded context budgets.
+  - Manual "Enrich Context" path must remain available for high-fidelity cases.
+- Preserve current Gemini-based behavior while introducing provider abstraction and new providers.
+- Secrets must never be committed to git; use environment variables and local non-committed config files only.
+
+## Guardrail Enforcement
+- Run `npm run check:guardrails` before merge.
+- `scripts/check-guardrails.sh` blocks:
+  - Deferred-scope artifacts (Capacitor/iOS/Android/Jira paths and dependencies).
+  - Committed secrets and tracked environment files.
+  - Missing explicit feature-flag declarations (`ENABLE_OPENAI_WRITER`, `ENABLE_ANTHROPIC_WRITER`, `ENABLE_MCP_CONTEXT`).
+  - Missing MCP context sanitization path (`server/mcp/sanitizeContext.ts`).
+
+## Local Verification
+- Lightweight guardrail check: `npm run check:guardrails`.
+- Full local verification flow: `npm run verify`.
diff --git a/package.json b/package.json
index 43475decb65122d796fd9869b2dd727c1981c848..a5ee74471b37088e979c9fe412d149bf26c0ce18
--- a/package.json
+++ b/package.json
@@ -5,6 +5,9 @@
   "type": "module",
   "scripts": {
     "dev": "vite",
+    "check:guardrails": "bash ./scripts/check-guardrails.sh",
+    "check": "npm run check:guardrails",
+    "verify": "npm run check && npm run build",
     "build": "vite build",
     "preview": "vite preview"
   },
diff --git a/scripts/check-guardrails.sh b/scripts/check-guardrails.sh
new file mode 100755
index 0000000000000000000000000000000000000000..7e156e6ebb0d75e8aad6607b6556374ba90988ee
--- /dev/null
+++ b/scripts/check-guardrails.sh
@@ -0,0 +1,123 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+cd "$ROOT_DIR"
+
+FAILED=0
+
+record_error() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n' "$1" >&2
+}
+
+record_error_block() {
+  FAILED=1
+  printf '[guardrails] ERROR: %s\n%s\n' "$1" "$2" >&2
+}
+
+require_file() {
+  local path="$1"
+  if [[ ! -f "$path" ]]; then
+    record_error "Missing required file: $path"
+  fi
+}
+
+list_tracked_files() {
+  git ls-files | rg -v ':Zone\.Identifier$' || true
+}
+
+check_forbidden_scope_artifacts() {
+  local path_matches
+  path_matches="$(list_tracked_files | rg -n '(^|/)(ios|android)(/|$)|(^|/)capacitor(\.|/|$)|(^|/)capacitor\.config\.(json|js|ts)$|(^|/)(jira|atlassian)(/|$)' || true)"
+  if [[ -n "$path_matches" ]]; then
+    record_error_block "Deferred-scope artifact paths detected." "$path_matches"
+  fi
+
+  local capacitor_deps
+  capacitor_deps="$(rg -n '"@capacitor/' package.json || true)"
+  if [[ -n "$capacitor_deps" ]]; then
+    record_error_block "Deferred-scope Capacitor dependency detected in package.json." "$capacitor_deps"
+  fi
+
+  local jira_deps
+  jira_deps="$(rg -n '"(@atlassian/|jira|jira-client)"' package.json || true)"
+  if [[ -n "$jira_deps" ]]; then
+    record_error_block "Deferred-scope Jira/Atlassian dependency detected in package.json." "$jira_deps"
+  fi
+}
+
+check_secret_hygiene() {
+  local tracked_env_files
+  tracked_env_files="$(list_tracked_files | rg -n '(^|/)\.env(\..+)?$' | rg -v '\.env\.example$' || true)"
+  if [[ -n "$tracked_env_files" ]]; then
+    record_error_block "Tracked environment file(s) detected; use local, uncommitted env files." "$tracked_env_files"
+  fi
+
+  local secret_matches
+  secret_matches="$(git grep -nE 'AIza[0-9A-Za-z_-]{35}|sk-ant-[A-Za-z0-9_-]{20,}|sk-[A-Za-z0-9]{20,}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]{10,}' || true)"
+  if [[ -n "$secret_matches" ]]; then
+    record_error_block "Possible committed secret(s) detected." "$secret_matches"
+  fi
+}
+
+check_feature_flags() {
+  require_file "config/featureFlags.ts"
+  require_file ".env.example"
+
+  local missing_entries=()
+  local flag
+  for flag in ENABLE_OPENAI_WRITER ENABLE_ANTHROPIC_WRITER ENABLE_MCP_CONTEXT; do
+    if ! rg -q "$flag" config/featureFlags.ts; then
+      missing_entries+=("config/featureFlags.ts missing ${flag}")
+    fi
+
+    if ! rg -q "^${flag}=false$" .env.example; then
+      missing_entries+=(".env.example missing ${flag}=false")
+    fi
+  done
+
+  if ((${#missing_entries[@]} > 0)); then
+    local details=""
+    local item
+    for item in "${missing_entries[@]}"; do
+      details+="- ${item}"$'\n'
+    done
+    record_error_block "Explicit feature-flag guardrails are incomplete." "$details"
+  fi
+}
+
+check_mcp_sanitization_path() {
+  require_file "server/mcp/sanitizeContext.ts"
+
+  if [[ -f "server/mcp/sanitizeContext.ts" ]]; then
+    if ! rg -q 'export function sanitizeMcpContext' server/mcp/sanitizeContext.ts; then
+      record_error "server/mcp/sanitizeContext.ts must export sanitizeMcpContext."
+    fi
+
+    if ! rg -q 'source' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain source-tagged."
+    fi
+
+    if ! rg -q 'maxChars|DEFAULT_MAX_CHARS|truncated' server/mcp/sanitizeContext.ts; then
+      record_error "MCP sanitization output must remain bounded and truncation-aware."
+    fi
+  fi
+}
+
+check_guardrail_docs() {
+  require_file "docs/guardrails.md"
+}
+
+check_guardrail_docs
+check_forbidden_scope_artifacts
+check_secret_hygiene
+check_feature_flags
+check_mcp_sanitization_path
+
+if [[ "$FAILED" -ne 0 ]]; then
+  printf '[guardrails] Validation failed.\n' >&2
+  exit 1
+fi
+
+printf '[guardrails] All guardrail checks passed.\n'
diff --git a/server/mcp/sanitizeContext.ts b/server/mcp/sanitizeContext.ts
new file mode 100644
index 0000000000000000000000000000000000000000..c57ff45cc1009bf11bc85ec5537cf99ae4ae2a45
--- /dev/null
+++ b/server/mcp/sanitizeContext.ts
@@ -0,0 +1,60 @@
+export interface McpSourceTag {
+  serverId: string;
+  serverName?: string;
+  resource?: string;
+}
+
+export interface McpRawContextPayload {
+  source: McpSourceTag;
+  content: unknown;
+}
+
+export interface SanitizedMcpContextSnippet {
+  source: McpSourceTag;
+  text: string;
+  truncated: boolean;
+}
+
+const DEFAULT_MAX_CHARS = 4_000;
+const MARKUP_PATTERN = /<[^>]*>/g;
+const CONTROL_CHAR_PATTERN = /[\u0000-\u001f\u007f]/g;
+const WHITESPACE_PATTERN = /\s+/g;
+
+const toText = (value: unknown): string => {
+  if (typeof value === "string") {
+    return value;
+  }
+
+  if (value === null || value === undefined) {
+    return "";
+  }
+
+  try {
+    return JSON.stringify(value, null, 2);
+  } catch {
+    return String(value);
+  }
+};
+
+const normalizeText = (input: string): string => {
+  return input
+    .replace(MARKUP_PATTERN, " ")
+    .replace(CONTROL_CHAR_PATTERN, " ")
+    .replace(WHITESPACE_PATTERN, " ")
+    .trim();
+};
+
+export function sanitizeMcpContext(
+  payload: McpRawContextPayload,
+  maxChars = DEFAULT_MAX_CHARS,
+): SanitizedMcpContextSnippet {
+  const budget = Number.isFinite(maxChars) && maxChars > 0 ? Math.floor(maxChars) : DEFAULT_MAX_CHARS;
+  const normalized = normalizeText(toText(payload.content));
+  const bounded = normalized.slice(0, budget);
+
+  return {
+    source: payload.source,
+    text: bounded,
+    truncated: normalized.length > bounded.length,
+  };
+}

tokens used
32,870
